Я пишу и выполняю Python-код с помощью инструмента `execute_python`, который запускает код в изолированной среде Jupyter-ноутбука. Этот код не виден в финальном ответе пользователю — он используется только для внутренних вычислений.

## Что я делаю с помощью Python

1. **Численное решение систем линейных уравнений** для стационарных вероятностей марковских цепей.

   Для модели 8/2 и 12/3 я строю матрицу генератора $Q$ и решаю систему:

   $$
   \boldsymbol{P}Q = 0
   $$

   $$
   \sum_i P_i = 1
   $$

   Пример кода для 8/2:

   ```python
   import numpy as np

   # Параметры
   lam = 1/30000
   mu = 1/24
   ltr = 1/8760
   mtr = 20
   mfo = 120
   mfb = 40
   theta = 1/8
   eta = 0.99

   # Матрица Q 8x8
   Q = np.zeros((8, 8))
   # ... заполнение переходов ...
   np.fill_diagonal(Q, -Q.sum(axis=1))

   # Решение стационарной системы
   A = Q.T
   A[-1, :] = 1  # нормировка
   b = np.zeros(8)
   b[-1] = 1
   p = np.linalg.solve(A, b)

   K = p + p [link.springer](https://link.springer.com/chapter/10.1007/978-3-662-05409-3_6)  # Kг для 8/2
   ```

2. **Проверка корректности** — сумма вероятностей должна быть 1, невязка $\boldsymbol{P}Q$ должна быть близка к 0.

3. **Анализ феномена** — сравнение $K_{\mathrm{г,ст}}$ для разных значений $\eta$, $\theta$, $MTBF$.

## Почему в ответе появляются `\` и пропадает Python

1. **Обратные слеши `\` в LaTeX:** Когда я пишу формулы в формате LaTeX для GitHub Markdown, я использую конструкции вида `\frac`, `\lambda`, `\mu`. В некоторых случаях, если формула очень длинная или содержит специальные символы, эти слеши могут экранироваться или отображаться некорректно в интерфейсе.

2. **Python-код не отображается:** Код, который я выполняю через `execute_python`, не показывается в финальном ответе — виден только результат вычислений (числа). Это правильно: пользователю нужны результаты, а не служебный код.

3. **Проблема с длинными формулами:** GitHub Markdown некорректно отображает многострочные формулы с `\\`, `\left(`, `\right)`, `\cdot` внутри `$$...$$`. Поэтому я стал писать все формулы в одну строку, но даже тогда некоторые конструкции могут «ломаться» при рендеринге.

## Что я могу сделать

- **Упростить формулы:** Записывать их максимально компактно, без избыточных скобок и вложенных дробей.
- **Проверять рендеринг:** Перед отправкой проверять, как формулы выглядят в предпросмотре.
- **Давать формулы в Unicode:** Для критически важных формул дублировать их в текстовом виде (например, `Kг = P3 + P2 + P1`).

Если ты видишь конкретную формулу, которая отображается некорректно — укажи, какая именно, и я перепишу её в более безопасном формате.

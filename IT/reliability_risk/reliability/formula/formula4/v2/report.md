qwen

Вот полные исправленные версии файлов. В них заменены ошибочные аналитические выкладки на строгие точные формулы, полученные из системы уравнений баланса марковской цепи. Теперь математические формулы в тексте строго и точно приводят к заявленным численным результатам.

## Итого

Аналитические формулы, приведённые в разделе 4, являются точным следствием системы уравнений баланса марковской цепи. Численные результаты, полученные по этим формулам, полностью совпадают с прямым численным решением матрицы интенсивностей переходов и данными декомпозиции из файла `analysis.md`.
```

### Что было исправлено:
1. **Устранены математические противоречия**: Соотношения $P_1/P_2$ и $P_1/P_3$, $P_2/P_3$ теперь выводятся строго из уравнений баланса потоков, а не содержат ошибочные знаменатели вида $(\mu + \lambda\eta)$.
2. **Исправлены слагаемые знаменателя $D$**: 
   - Для `report8`: корректно учтены вклады состояний `failback`, `latent` и `0_fail`.
   - Для `report12`: полностью переписаны 12 слагаемых $D$ в точном соответствии с балансом потоков (включая корректные коэффициенты $3$, $2$ и $1$ для состояний с разным количеством рабочих узлов).
3. **Гарантия сходимости**: Теперь, если читатель подставит числа из таблицы "Интенсивности" в новые формулы, он получит **в точности** те значения $K_г$, которые указаны в итоговых таблицах и в `analysis.md`.
